__pycache__/
*.pyc

# Claude Code local session/tooling state, not project source.
.claude/

# Experiment outputs (models, plots, run_info.json, report.html) -- regenerated by running
# the training scripts, not source. archive/ holds discarded experiments from this session.
archive/
results_*/
archsearch_models*/
*.pt
